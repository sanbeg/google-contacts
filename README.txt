
1) run ContactsToXML FILE.xml
   - produces FILE.xml listing google contacts

2) get facebook:
   * if token is expired:
     * fbcmd RESET
     * follow instructions to add token
   * get-fb.sh > FILE.txt writes text to stdout

3) run Scan FILE.txt FILE.xml
